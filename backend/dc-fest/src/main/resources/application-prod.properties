# Server port
server.port=${BACKEND_PORT}

# Database configuration
spring.datasource.url=${BACKEND_DB_URL}
spring.datasource.username=${BACKEND_DB_USERNAME}
spring.datasource.password=${BACKEND_DB_PASSWORD}

# JWT Configurations
jwt.secret.key=${BACKEND_DB_SECRET}

file.upload-dir=${UPLOAD_FILE_DIRECTORY}


# Enable SQL logging
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Log SQL statements and parameters
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE